<?php
declare(strict_types=1);

namespace Civi;

/**
 * Отказ по бизнес-правилу, который нужно показать человеку:
 * «в категории есть технологии», «параметры неJSON» и подобное.
 *
 * Отдельный класс нужен именно для того, чтобы ловить такие случаи
 * прицельно. Раньше они ловились как RuntimeException — а от него
 * наследуется и PDOException, поэтому любая ошибка запроса молча
 * превращалась в редирект на главную вместо честного сообщения.
 */
final class UserError extends \RuntimeException
{
}
